name: Document Submit Latency Alert
on:
  workflow_dispatch:

jobs:
  handle-alert:
    runs-on: ubuntu-latest
    
    steps:      
    - name: Parse Alert Data
      id: parse
      shell: pwsh
      run: |
        $alert =  '${{ toJson(github.event.inputs) }}' | ConvertFrom-Json
        $alert